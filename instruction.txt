$ python3 -m venv .venv
$ source ./.venv/bin/activate
$ pip install -r requirements.txt
$ export GITHUB_TOKEN=ghp_CHWL220nF5Em4DnaI6*********d0cldPG

## TO FREEZE ENVIRONMENT
python3 gitops.py --target-env dev --action pause

kubectl apply -f git-repo-secret.yaml
kubectl apply -f apps-dev.yaml

